# TeamFlow Database

This folder is reserved for PostgreSQL setup files, migrations, seed data, and local database notes.

The `init` folder is mounted into the PostgreSQL container by `docker-compose.yml`.

